#include "holberton.h"

/**
 * _abs - returns absolute number value
 * @n: number value
 * Return: absolute numerical value
 */

int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
